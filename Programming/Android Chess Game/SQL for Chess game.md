```sql
DROP TABLE boards
DROP TABLE games

CREATE TABLE games(
_id INTEGER PRIMARY KEY AUTO_INCREMENT,
dt DATETIME DEFAULT CURRENT_TIMESTAMP,
playerWhite VARCHAR(30),
playerBlack VARCHAR(30)
);

CREATE TABLE boards(
_id INTEGER PRIMARY KEY AUTO_INCREMENT,
game INTEGER REFERENCES games(_id),
dt DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
a1 INTEGER,
b1 INTEGER,
c1 INTEGER,
d1 INTEGER,
e1 INTEGER,
f1 INTEGER,
g1 INTEGER,
h1 INTEGER,
a2 INTEGER,
b2 INTEGER,
c2 INTEGER,
d2 INTEGER,
e2 INTEGER,
f2 INTEGER,
g2 INTEGER,
h2 INTEGER,
a3 INTEGER,
b3 INTEGER,
c3 INTEGER,
d3 INTEGER,
e3 INTEGER,
f3 INTEGER,
g3 INTEGER,
h3 INTEGER,
a4 INTEGER,
b4 INTEGER,
c4 INTEGER,
d4 INTEGER,
e4 INTEGER,
f4 INTEGER,
g4 INTEGER,
h4 INTEGER,
a5 INTEGER,
b5 INTEGER,
c5 INTEGER,
d5 INTEGER,
e5 INTEGER,
f5 INTEGER,
g5 INTEGER,
h5 INTEGER,
a6 INTEGER,
b6 INTEGER,
c6 INTEGER,
d6 INTEGER,
e6 INTEGER,
f6 INTEGER,
g6 INTEGER,
h6 INTEGER,
a7 INTEGER,
b7 INTEGER,
c7 INTEGER,
d7 INTEGER,
e7 INTEGER,
f7 INTEGER,
g7 INTEGER,
h7 INTEGER,
a8 INTEGER,
b8 INTEGER,
c8 INTEGER,
d8 INTEGER,
e8 INTEGER,
f8 INTEGER,
g8 INTEGER,
h8 INTEGER
);

CREATE TABLE chat(
_id INTEGER PRIMARY KEY AUTO_INCREMENT,
game INTEGER REFERENCES games(_id),
dt DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
player VARCHAR(30),
message VARCHAR(200)
);
```
